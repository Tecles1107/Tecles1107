{
  "name": "studio-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run start --prefix backend\" \"serve frontend -l 8080\"",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "serve": "^14.2.0",
    "prettier": "^3.0.0"
  }
}
